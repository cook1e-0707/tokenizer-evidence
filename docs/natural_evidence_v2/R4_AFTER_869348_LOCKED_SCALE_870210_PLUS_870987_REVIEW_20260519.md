# R4 After-869348 Locked-Scale 870210 + 870987 Review - 2026-05-19

## Decision

The R4 Qwen same-contract first-token event locked-scale generation package
passed the precommitted locked-scale gate after resuming the failed/missing
shards from job `870210` with resume job `870987`.

This is a Qwen-only provider-side first-token event trace result. It is not a
text-only phrase decoder success and does not unlock paper-facing claims by
itself.

## Jobs

```text
source job:
  870210, nat-ev-v2-r4lGen
  completed 79/96 shards
  failed/missing shards: 75, 76, 78, 82-95
  failure class: runtime/storage failure

resume job:
  870987, nat-ev-v2-r4lGen
  array: 75,76,78,82-95%6
  completed 17/17 shards
```

## Aggregate Artifact

```text
results/natural_evidence_v2/status/r4_after_869348_locked_scale_generation_870210_plus_870987_aggregate_20260519/
```

Aggregation status:

```text
PASS_R4_AFTER_869348_LOCKED_SCALE_GENERATION_GATE
```

## First-Token Event Gate

```text
complete shards: 96/96
generated rows: 294912
protected strict accepts: 94/96
protected accepts ignoring quality: 94/96
raw accepts: 0/96
task-only accepts: 0/96
wrong-key accepts: 0/96
wrong-payload accepts: 0/96
first-token forbidden public surface count: 0
within-block duplicate response hash count: 0
global duplicate response hash extra rows: 0
duplicate generation id extra rows: 0
unique response hashes: 294912/294912
trace binding checked rows: 294912
trace binding invalid rows: 0
```

The result passes the locked-scale first-token event criteria:

```text
protected strict accepts >= 85/96
protected ignoring-quality accepts >= 90/96
all control accepts = 0/96
global duplicate response hash extra rows = 0
trace binding validity = 100%
```

## Phrase Decoder Status

The full phrase decoder remains report-only and did not recover the channel:

```text
full phrase protected accepts, format_scrub=all: 0/96
full phrase protected accepts, format_scrub=none: 0/96
```

The current positive evidence is therefore scoped to provider-side bound
first-token event traces, not text-only phrase-level recovery from final text.

## Claim Control

Allowed internal statement:

```text
R4 Qwen same-contract first-token event locked-scale generation passed under
bound trace decoding, with protected 94/96 and all diagnostic control arms 0/96.
```

Still not allowed:

```text
paper-facing positive claim
text-only watermark success
full phrase decoder success
FAR aggregation
payload diversity
Llama/cross-family success
same-family null claim
sanitizer robustness
```

