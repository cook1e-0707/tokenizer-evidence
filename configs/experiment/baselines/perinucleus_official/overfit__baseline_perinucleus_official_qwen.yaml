schema_name: baseline_perinucleus_official_overfit
schema_version: 1
package_name: baseline_perinucleus_official_overfit
status: overfit_pending

official_repo:
  url: https://github.com/SewoongLab/scalable-fingerprinting-of-llms
  local_path: external_baselines/scalable_fingerprinting_official
  commit_hash: fdceaba14bd3e89340916a6a40e27c945d48460e
  license: MIT

overfit_gate:
  scratch_root: /hpcstor6/scratch01/g/guanjie.lin001/tokenizer-evidence/baselines/perinucleus_official_overfit
  model: Qwen/Qwen2.5-7B-Instruct
  seed: 17
  key_length: 16
  response_length: 1
  generation_batch_size: 1
  nucleus_t: 0.8
  nucleus_k: 3
  use_chat_template: true
  batch_size: 1
  learning_rate: 5.0e-5
  weight_decay: 0.0
  early_stop_ce: 0.01
  max_sequence_length: 64
  lora:
    rank: 64
    alpha_ratio: 2.0
    dropout: 0.0
    target_modules:
      - q_proj
      - k_proj
      - v_proj
      - o_proj
      - gate_proj
      - up_proj
      - down_proj
  stages:
    - name: stage1_one_fingerprint
      num_fingerprints: 1
      max_epochs: 100
    - name: stage2_four_fingerprints
      num_fingerprints: 4
      max_epochs: 100
    - name: stage3_sixteen_fingerprints
      num_fingerprints: 16
      max_epochs: 100

repo_outputs:
  doc: docs/baseline_perinucleus_official_overfit.md
  table: results/tables/baseline_perinucleus_official_overfit.csv
  summary: results/processed/paper_stats/baseline_perinucleus_official_overfit_summary.json
  compute: results/processed/paper_stats/baseline_perinucleus_official_overfit_compute.json

runtime:
  run_id: null
  output_dir: null
