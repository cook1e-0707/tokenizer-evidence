runtime:
  output_root: results/raw
  launcher_mode: slurm
  resources:
    partition: pomplun
    qos: pomplun
    gpu_type: h200
    num_gpus: 1
    cpus: 4
    mem_gb: 120
    time_limit: "02:00:00"
    account: null
    environment_setup: |
      source ~/.bashrc
      # activate your environment here
