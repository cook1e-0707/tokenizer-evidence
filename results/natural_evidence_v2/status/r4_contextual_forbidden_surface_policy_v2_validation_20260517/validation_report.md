# R4 Contextual Forbidden-Surface Policy V2 Validation

Status: `PASS_R4_CONTEXTUAL_FORBIDDEN_SURFACE_POLICY_V2_VALIDATION_NO_SUBMIT`

This validation is artifact-only and does not rescue `868260`.

- ordinary physical `bucket` examples are reported as ordinary-domain literals.
- technical `bucket`/fingerprint/watermark/payload/decoder/hidden-signal examples remain fatal.
- ambiguous `bucket` remains fatal until a precommitted context rule resolves it.
