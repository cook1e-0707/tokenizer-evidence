# R4 Coverage-Scale Submission Record

Submitted one H200/pomplun Slurm job:

```text
job_id: 864761
job_name: nat-ev-v2-r4mof
allowlist_entry: v2_r4_candidate_v3_coverage_scale_micro_overfit_h200
partition: pomplun
qos: pomplun
account: cs_yinxin.wan
```

The allowlist entry was disabled immediately after `sbatch` returned. Local and
remote post-submit allowlist safety checks both passed with zero enabled
entries.

This submission is teacher-forced protected training/scoring only. It does not
start generation, Qwen E2E, Llama, same-family null, sanitizer, FAR,
payload-diversity claims, or paper-facing positive claims.
