# Hourly natural_evidence_v1 report

## Current phase
QWEN_ACTUAL_PREFIX_REFERENCE_SCORING_READY_BLOCKED_BY_CHIMERA_DNS.

Closest controlling spec: `docs/specs/stage4_real_integration_spec.md`, with
natural-output overrides in `docs/natural_evidence_v1/README.md` and
`docs/natural_evidence_v1/protocol_commitment.md`.

## Jobs
Local Slurm tools are unavailable: `squeue` and `sacct` are not installed in
this worktree environment.

`ssh -G chimera` still resolves the alias to `hostname chimerahead.umb.edu` with
`ConnectTimeout 10` and `ConnectionAttempts 3`, but `ssh chimera` failed DNS
resolution on the first attempt and again after the documented DNS warm-up plus
three non-interactive retries.

Current Chimera `squeue`, `sacct`, and remote output-existence checks are
therefore unverified. No Slurm job was submitted.

## Artifacts found
Local `natural_evidence_v1` source/config/docs/status paths are present.

The required local Phase A JSONL/bank paths under `results/natural_evidence_v1`
are missing in this worktree:

- `reference_outputs/qwen_reference_outputs.jsonl`
- `reference_outputs/llama_reference_outputs.jsonl`
- `reference_candidates/qwen_topk_candidates.jsonl`
- `reference_candidates/llama_topk_candidates.jsonl`
- `bucket_banks/qwen_bucket_bank_entries.jsonl`
- `bucket_banks/llama_bucket_bank_entries.jsonl`

`/hpcstor6` is not mounted locally, so remote Phase A and diagnostic artifacts
remain last-known from persistent state, not freshly verified this run.

Local actual-prefix plan artifacts are present in status copies. The job 844494
manifest records 57,164 Qwen actual-prefix scoring rows from 14,336 retained
generated outputs, with the recommended first scoring job set to Qwen,
candidate_top_k=64, bucket_count=4, and training_allowed=false.

The local actual-prefix reference-scoring wrapper passed `bash -n` and contains
the required Slurm mail directives. It is dry-run by default and requires both
`START_ACTUAL_PREFIX_SCORING=1` and `DRY_RUN_ONLY=0` before model scoring.

## Gate status
| Gate | Status | Evidence | Action |
|---|---|---|---|
| Repo state inspected | PASS | branch=`main`; head=`e58308a Set natural bank target to 24000`; worktree dirty from prior work | Do not revert unrelated changes |
| Old-route keyword scan | NO_RED_FLAG | Matches are denylist config, guardrail tests, status/docs text, and payload variable names; no new carrier-block mainline found | Continue to block explicit evidence-block outputs |
| Local Phase A artifacts | MISSING_LOCAL | Required JSONL/bank files are absent locally | Use Chimera once reachable; do not infer fresh completion |
| Chimera access | BLOCKED_DNS | first `ssh chimera` plus three documented retries failed resolving `chimerahead.umb.edu` | Retry next run before any submission |
| Qwen actual-prefix scoring plan | PASS_READY | local manifest records 57,164 scoring prefixes; wrapper reviewed and allowlisted | Submit only after remote queue/output checks pass |
| Qwen actual-prefix reference scoring submission | BLOCKED_NOT_SUBMITTED | remote `squeue`/`sacct` and output-existence checks unverified | No GPU job this run |
| Protected LoRA/E2E training | BLOCKED | actual-prefix scoring and audit still NEEDS_RESULTS | Do not train |
| Llama / Qwen 8-way / full matrix | BLOCKED | Qwen diagnostic actual-prefix path incomplete | Do not start |

## Actions executed this run
- Read persistent docs/config/state and the automation memory.
- Ran `git status --short`, `git branch --show-current`, and
  `git log -1 --oneline`.
- Scanned `natural_evidence_v1` paths for old explicit-route keywords.
- Checked local Phase A artifact paths for existence, line counts, assistant
  contamination, and old-route keyword hits.
- Checked local Slurm tool availability and `/hpcstor6` mount state.
- Ran `ssh chimera` and then the documented DNS warm-up plus three retry
  attempts.
- Ran local `bash -n` on the actual-prefix reference-scoring Slurm wrapper.
- Wrote status/report bookkeeping only.

No experiment state-changing action was executed. No GPU scoring, training,
E2E evaluation, Llama job, Qwen 8-way job, full matrix, FAR aggregation, or
paper claim edit was executed.

## Red flags
- Chimera DNS remains unreachable after canonical alias verification and retry.
- Remote queue and remote output-existence checks are unverified this run.
- Local Phase A JSONL/bank artifacts are missing and `/hpcstor6` is not mounted
  locally.
- The worktree is dirty from prior automation/user changes; this run did not
  revert or overwrite them.

## Next allowed action
Retry Chimera access. If `ssh chimera`, `squeue`, `sacct`, and remote
output-existence checks succeed, and no conflicting `natural_evidence_v1` job or
completed actual-prefix scoring output exists, submit exactly one allowlisted
Qwen reference-model top-k scoring job with:

- `scripts/natural_evidence_v1/slurm/actual_prefix_reference_scoring.sbatch`
- `START_ACTUAL_PREFIX_SCORING=1`
- `DRY_RUN_ONLY=0`
- candidate_top_k=64
- bucket_count=4
- 57,164-row actual-prefix scoring input from job 844494

This is reference-model scoring only, not training.

## Claims still forbidden
- 24,576 fingerprints
- stealth guarantee
- full FAR
- robust to all paraphrasing
- cross-family generality
- superiority over Scalable/Perinucleus

## State update
Current phase remains
QWEN_ACTUAL_PREFIX_REFERENCE_SCORING_READY_BLOCKED_BY_CHIMERA_DNS.
Last checked time is 2026-05-06T11:04:02Z. Next allowed action remains a
single allowlisted Qwen actual-prefix reference-scoring GPU job only after
Chimera queue/output checks pass. Remaining NEEDS_RESULTS are unchanged.
