# Hourly natural_evidence_v1 report

## Current phase
QWEN_DIAGNOSTIC_E2E_EVAL_RECOVERY_SUBMITTED_PENDING.

The paper-ready held-out density/effective-capacity gate remains failed. All E2E
work here is diagnostic high-risk only and cannot support paper-facing claims.

## Jobs
- 843480 `nat-ev-qwen-diag-eval`: FAILED after 01:43:40, exit code `0:15`;
  output directory contained only preflight JSON.
- 844090 `nat-ev-qwen-diag-eval`: PENDING; reason: nodes required for the job
  are down, drained, or reserved for higher-priority partitions.

## Artifacts found
- Job 843480 artifacts:
  `qwen_diagnostic_e2e_eval_preflight.json` only.
- No 843480 summary, decode trace, bucket observations, or generated-output
  JSONL exists.
- Qwen diagnostic LoRA checkpoints from job 843029 remain present for
  protected/task-only LoRA over P0421/P1729 and seeds 17/23.

## Gate status
| Gate | Status | Evidence | Action |
|---|---|---|---|
| Qwen diagnostic training | PASS | job 843029 completed; 8 checkpoints audited | keep |
| Qwen diagnostic eval job 843480 | FAIL_OPERATIONAL_NO_RESULTS | `sacct` FAILED `0:15`, no traceback, MaxRSS about 19.7GB, preflight-only output | recovery eval needed |
| Eval recovery script | PASS_PREFLIGHT | local tests/static validation passed; remote static validation passed after syncing stale wrapper | submitted one recovery job |
| Qwen diagnostic eval recovery job 844090 | PENDING | `squeue` pending for node availability | monitor only |
| Paper-ready density | FAIL | held-out density 0.43255/100 tokens; effective bits/response 0.46897 | no paper-ready claim |
| Llama / 8-way / full matrix | BLOCKED | Qwen diagnostic eval not reviewed | do not launch |

## Actions executed this run
- Checked automation state and Chimera Slurm status.
- Diagnosed job 843480 as an operational failure with no result artifacts.
- Updated `evaluate_diagnostic_e2e.py` to write generated outputs, observations,
  decode rows, and progress JSON incrementally after raw and each
  payload/seed/arm unit.
- Ran local validation: `.venv/bin/python -m pytest tests/test_natural_evidence_v1.py -q`
  passed with 31 tests; static validation, `py_compile`, and `git diff --check`
  passed.
- Synced the repaired eval script and Slurm wrappers to Chimera.
- Fixed remote static validation blocker from a stale duplicate wrapper lacking
  Slurm mail directives.
- Submitted exactly one allowlisted recovery GPU eval job: 844090.

## Red flags
- No payload recovery, verifier acceptance, or null rejection result exists yet.
- Job 843480 was terminated by signal 15 without traceback; recovery job 844090
  must be monitored before any additional eval/training action.
- Do not aggregate FAR from partial or incomplete outputs.

## Next allowed action
Monitor recovery job 844090 only. After it completes or fails, audit
`qwen_diagnostic_e2e_eval_progress.json`, `qwen_diagnostic_decode_trace.csv`,
and `qwen_diagnostic_e2e_eval_summary.json` if present. Do not submit another
eval, any additional training, Llama, Qwen 8-way, or full matrix job while
844090 is pending/running.

## Claims still forbidden
- 24,576 fingerprints
- stealth guarantee
- full FAR
- robust to all paraphrasing
- cross-family generality
- superiority over Scalable/Perinucleus

## State update
`AUTOMATION_STATE.md` and `gate_status.json` updated to
`QWEN_DIAGNOSTIC_E2E_EVAL_RECOVERY_SUBMITTED_PENDING`; job 843480 recorded as
failed operationally and job 844090 recorded as pending.
