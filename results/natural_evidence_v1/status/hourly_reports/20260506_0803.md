# Hourly natural_evidence_v1 report

## Current phase
QWEN_ACTUAL_PREFIX_REFERENCE_SCORING_READY_BLOCKED_BY_CHIMERA_DNS

The next reviewed action is still Qwen actual-prefix reference-model top-k
scoring. This run did not submit it because Chimera DNS failed before active
Slurm jobs and remote output existence could be verified.

## Jobs
- No new Slurm job was submitted.
- Local `squeue` and `sacct` are not installed.
- `ssh -G chimera` shows the alias points to `chimerahead.umb.edu` with
  `ConnectionAttempts=3` and `ConnectTimeout=10`.
- `ssh chimera` failed DNS resolution on the first attempt and all three
  documented non-interactive retries.
- Current Chimera queue state is unverified. Last known relevant job remains
  844494 (`nat-ev-qwen-aplan`), completed 0:0 in the prior verified state.

## Artifacts found
- Local status/report artifacts exist under `results/natural_evidence_v1/status`.
- Local Phase A JSONL paths under `results/natural_evidence_v1/reference_outputs`,
  `reference_candidates`, and `bucket_banks` are missing or empty in this
  worktree.
- `/hpcstor6` is not mounted locally, so remote Phase A outputs and actual-prefix
  scoring outputs were not reverified this run.
- Last-known actual-prefix scoring plan from job 844494 remains 57164 scoring
  prefixes from 14336 retained generated outputs.
- `scripts/natural_evidence_v1/slurm/actual_prefix_reference_scoring.sbatch`
  includes the required Slurm mail directives and remains allowlisted as the
  only enabled GPU action.

## Gate status
| Gate | Status | Evidence | Action |
|---|---|---|---|
| Repo status | DIRTY_EXPECTED | existing natural_evidence_v1 worktree changes remain | Do not revert unrelated work |
| Old-route keyword scan | NO_RED_FLAG | matches are guardrail tests only, not a carrier-block output path | Continue natural route only |
| Static validation | PASS | `python3 scripts/natural_evidence_v1/validate_static.py --config configs/natural_evidence_v1/pilot.yaml --summary /private/tmp/natural_evidence_v1_static_validation_20260506_current.json` passed | Keep wrapper eligible |
| Wrapper syntax | PASS | `py_compile` and `bash -n` passed for the scorer/wrapper | No code fix needed |
| Chimera access | BLOCKED_DNS | canonical alias check passed, but `ssh chimera` failed DNS after retries | Do not submit job this run |
| Phase A local artifacts | MISSING_LOCAL | required local JSONL/bank files are absent or empty | Treat remote artifacts as unverified this run |
| Qwen actual-prefix reference scoring | READY_BUT_NOT_SUBMITTED | allowlist action enabled; remote queue/output state unverified | Retry Chimera, then submit one scoring job only if clear |
| Qwen diagnostic E2E | BLOCKED | actual-prefix reference scoring, compatibility filtering, bucketization, and audit absent | Do not rerun eval |
| Llama / 8-way / full matrix | BLOCKED | Qwen actual-prefix repair unresolved | Do not start |

## Actions executed this run
- Read automation memory, repo guardrails, README, automation state, protocol
  commitment, run allowlist, and gate status.
- Ran `git status --short`, branch, and last commit checks.
- Searched natural_evidence_v1 paths for old explicit evidence-block keywords.
- Checked local Phase A artifact paths and `/hpcstor6` mount status.
- Attempted Chimera Slurm checks through `ssh chimera`; DNS failed after
  documented retries.
- Ran local static validation, `py_compile`, and `bash -n`.
- Wrote this status report and refreshed persistent state only.

No experiment state-changing action was executed. No GPU scoring job, training,
E2E eval, Llama job, Qwen 8-way job, full matrix, FAR aggregation, or paper
claim edit was executed.

## Red flags
- Chimera active jobs and remote scoring output state are unverified this run.
- Do not submit Qwen actual-prefix scoring until `ssh chimera`, `squeue`, and
  remote output existence checks succeed.
- Static-bucket salvage has already failed with no payload recovery and low
  protected symbol rate; actual-prefix reference scoring is the required repair
  path before any diagnostic E2E rerun.
- No paper-facing Qwen E2E gate is passed, and no full FAR result exists.

## Next allowed action
Retry Chimera access. If Slurm shows no conflicting active natural_evidence_v1
job and the actual-prefix scoring output directory has no completed output,
submit exactly one allowlisted GPU scoring job:

```bash
START_ACTUAL_PREFIX_SCORING=1 DRY_RUN_ONLY=0 \
sbatch scripts/natural_evidence_v1/slurm/actual_prefix_reference_scoring.sbatch
```

This is Qwen reference-model candidate scoring only: candidate_top_k=64,
bucket_count=4, no training, no E2E eval, no payload-recovery claim.

## Claims still forbidden
- 24,576 fingerprints
- stealth guarantee
- full FAR
- robust to all paraphrasing
- cross-family generality
- superiority over Scalable/Perinucleus

## State update
`current_phase` remains
`QWEN_ACTUAL_PREFIX_REFERENCE_SCORING_READY_BLOCKED_BY_CHIMERA_DNS`.
`next_allowed_action` remains
`retry_chimera_then_submit_one_allowlisted_qwen_actual_prefix_reference_scoring_gpu_job_topk64_no_training`.
`last_state_changing_action` records that no experiment state-changing action
was executed during this run.
