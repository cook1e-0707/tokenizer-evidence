# Hourly natural_evidence_v1 report

## Current phase
QWEN_ACTUAL_PREFIX_MISSING_BUCKET_REPAIR_PLAN_READY.

The next step was a local missing-compatible-bucket diagnosis using completed
job `845284` outputs. No new Slurm job was submitted.

## Jobs
- `845284` remains the latest completed compute job: `COMPLETED 0:0`.
- No CPU/GPU task was run directly on Chimera.
- No training, E2E eval, Llama, Qwen 8-way, or full matrix was started.

## Artifacts found
- Local compatibility copies:
  `results/natural_evidence_v1/status/actual_prefix_suffix_compatibility_845284/`.
- New diagnosis outputs:
  `missing_bucket_diagnosis_summary.json`,
  `missing_bucket_by_bucket.csv`,
  `missing_bucket_by_condition.csv`,
  `missing_bucket_by_pattern.csv`,
  `missing_bucket_repair_plan.md`.

## Gate status
| Gate | Status | Evidence | Action |
|---|---|---|---|
| Suffix compatibility | FAIL_LOW_MIN1_MIN2 | min1=853; configured-min=117; probability-gated=59 | Keep training/E2E blocked |
| Missing compatible bucket | BLOCKER | missing_compatible_bucket=4109 | Diagnose repair path |
| Candidate slack in missing buckets | PRESENT_DIAGNOSTIC | 3259 entries have some unscored missing-bucket candidate slack; 995 have slack in every missing bucket | Higher-cap sensitivity is plausible |
| No-slack missing buckets | HARD_BLOCKER_SUBSET | 850 entries have no unscored missing-bucket slack | Selector repair likely still needed |
| Allowlist duplicate prevention | PASS | completed suffix compatibility action disabled | Avoid duplicate job |

## Actions executed this run
1. Copied the bucketized-candidates input and compatibility JSONL back locally.
2. Ran local-only missing-bucket diagnosis.
3. Wrote diagnosis JSON/CSV/MD artifacts.
4. Disabled the completed suffix-compatibility allowlist action.
5. Updated persistent state and this hourly report.

## Red flags
- The current outputs still do not support payload recovery, FAR, null rejection,
  or natural-output success claims.
- Higher-cap sensitivity is diagnostic only; it cannot be treated as a training
  gate until completed and audited.
- A no-slack subset remains, so candidate-cap increase may not fully repair the
  bank.

## Next allowed action
Prepare or review one bounded higher-cap suffix-compatibility sensitivity job.
If run, submit it through Slurm with a distinct output directory and explicit
`MAX_CANDIDATES_PER_BUCKET`; do not run CPU/GPU directly on Chimera.

## Claims still forbidden
- 24,576 fingerprints
- stealth guarantee
- full FAR
- robust to all paraphrasing
- cross-family generality
- superiority over Scalable/Perinucleus

## State update
Current phase advanced to
`QWEN_ACTUAL_PREFIX_MISSING_BUCKET_REPAIR_PLAN_READY`.
Training and E2E remain blocked.
