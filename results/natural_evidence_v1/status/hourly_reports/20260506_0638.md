# Hourly natural_evidence_v1 report

## Current phase
QWEN_ACTUAL_PREFIX_REFERENCE_SCORING_WRAPPER_READY_PENDING_SUBMISSION

This run implemented and reviewed the next GPU boundary. It did not submit a
Slurm job, did not run model scoring, did not train, and did not evaluate
payload recovery.

## Jobs
- No new Slurm job was submitted.
- Chimera access succeeded through `ssh chimera`, resolving to
  `chimerahead.umb.edu`.
- New/updated files were synced to `$HOME/tokenizer-evidence` on Chimera.

## Artifacts found
- Existing actual-prefix plan:
  `/hpcstor6/scratch01/g/guanjie.lin001/tokenizer-evidence/natural_evidence_v1/diagnostic_high_risk_qwen_e2e/eval/qwen_diagnostic_e2e_eval_844121/actual_prefix_scoring_plan_v1/actual_prefix_scoring_manifest.json`
- Existing scoring input:
  `/hpcstor6/scratch01/g/guanjie.lin001/tokenizer-evidence/natural_evidence_v1/diagnostic_high_risk_qwen_e2e/eval/qwen_diagnostic_e2e_eval_844121/actual_prefix_scoring_plan_v1/actual_prefix_scoring_input.jsonl`
- New local and remote scorer:
  `scripts/natural_evidence_v1/score_actual_prefix_reference_candidates.py`
- New local and remote wrapper:
  `scripts/natural_evidence_v1/slurm/actual_prefix_reference_scoring.sbatch`

## Gate status
| Gate | Status | Evidence | Action |
|---|---|---|---|
| Actual-prefix scoring plan | PASS_PLAN_COMPLETE | 57164 scoring prefixes from 14336 generated outputs | Use as scoring input |
| Actual-prefix reference scoring wrapper | PASS_REVIEWED_ALLOWLISTED_NO_TRAINING | local + remote static/py_compile/bash checks pass | Submit one GPU scoring job next |
| GPU allowlist | READY_ONE_ENABLED_ACTION | `qwen_actual_prefix_reference_model_scoring` enabled; old diagnostic eval disabled | Do not submit unlisted jobs |
| Qwen diagnostic E2E | BLOCKED | actual-prefix reference scoring/bucket audit absent | Do not rerun eval |
| Llama E2E | BLOCKED | Qwen actual-prefix repair incomplete | Do not start |

## Actions executed this run
- Added `scripts/natural_evidence_v1/score_actual_prefix_reference_candidates.py`.
- Added `scripts/natural_evidence_v1/slurm/actual_prefix_reference_scoring.sbatch`.
- Updated `configs/natural_evidence_v1/run_allowlist.yaml` so the new scoring
  action is the only enabled GPU action.
- Updated static validation and tests.
- Synced the changed files to Chimera and ran remote static/py_compile/bash
  validation.

## Red flags
- The next step uses GPU, but only for Qwen reference-model top-k scoring.
- Do not set `START_ACTUAL_PREFIX_SCORING=1` unless the command matches the
  allowlist and uses explicit top_k=64, bucket_count=4, input, output, and
  progress paths.
- No E2E eval or LoRA training is allowed until scoring, compatibility
  filtering, bucketization, and audit complete.

## Next allowed action
Submit exactly one allowlisted GPU scoring job:

```bash
START_ACTUAL_PREFIX_SCORING=1 DRY_RUN_ONLY=0 \
sbatch scripts/natural_evidence_v1/slurm/actual_prefix_reference_scoring.sbatch
```

This must run on the reviewed Qwen actual-prefix input with candidate_top_k=64
and bucket_count=4. It is reference scoring only, not training.

## Claims still forbidden
- 24,576 fingerprints
- stealth guarantee
- full FAR
- robust to all paraphrasing
- cross-family generality
- superiority over Scalable/Perinucleus

## State update
`current_phase` is
`QWEN_ACTUAL_PREFIX_REFERENCE_SCORING_WRAPPER_READY_PENDING_SUBMISSION`.
`next_allowed_action` is
`submit_allowlisted_qwen_actual_prefix_reference_scoring_gpu_job_topk64_no_training`.
