# Hourly natural_evidence_v1 report

## Current phase
QWEN_ACTUAL_PREFIX_HIGHCAP_SUFFIX_SENSITIVITY_RUNNING.

The expert decision was applied as one bounded diagnostic Slurm job. This is not
training, not E2E, not FAR, and not payload recovery.

## Jobs
- Submitted exactly one Slurm job: `845358` (`nat-ev-qwen-apcap`).
- Current status: `RUNNING` on `chimera13`.
- cap8 progress at the final check: 320 / 4996 entries, 6007 scored candidates,
  1218 compatible candidates.
- Account/QOS/GPU: `pi_yinxin.wan`, `scavenger_unlim`, `gres/gpu:A100:1`.
- No direct CPU/GPU compute was run on Chimera.

## Artifacts found
- Required inputs present on Chimera:
  - 14336 generated outputs from job `844121` retained evaluation.
  - 4996 top-k64 actual-prefix bucketized candidates.
  - cap=4 baseline summary and by-entry CSV from job `845284`.
- Sensitivity final outputs were absent before submission.

## Gate status
| Gate | Status | Evidence | Action |
|---|---|---|---|
| Missing-bucket repair plan | COMPLETE | 3259 entries have some unscored missing-bucket slack; 995 have full missing-bucket slack | Approved bounded sensitivity |
| High-cap sensitivity wrapper | PASS_VALIDATED | 40 tests, static validation, py_compile, bash -n, mail directives passed | Submitted one job |
| High-cap sensitivity job | RUNNING | job `845358`; cap8 progress 320/4996 | Monitor only |
| Training/E2E/Llama/full matrix | NOT_STARTED | only diagnostic sensitivity submitted | Keep blocked |

## Actions executed this run
1. Added `summarize_actual_prefix_suffix_sensitivity.py`.
2. Added `actual_prefix_suffix_highcap_sensitivity.sbatch`.
3. Added allowlist/static-validation/test coverage for the new diagnostic action.
4. Synced reviewed files to Chimera.
5. Submitted job `845358` with `CAP_VALUES=8,16`.

## Red flags
- This sensitivity job is diagnostic only. Even if cap helps, it is not a
  training gate without review.
- top-k128 and bucket_count=2 are not part of this submitted job; this job tests
  the minimal cap question on existing top-k64 4-way inputs.
- Do not submit another CPU/GPU task while `845358` is pending or running.

## Next allowed action
Monitor job `845358`. If complete, inspect cap=8/cap=16 outputs and the
sensitivity summary before deciding whether cap truncation or selector design is
the dominant blocker.

## Claims still forbidden
- 24,576 fingerprints
- stealth guarantee
- full FAR
- robust to all paraphrasing
- cross-family generality
- superiority over Scalable/Perinucleus

## State update
Current phase advanced to
`QWEN_ACTUAL_PREFIX_HIGHCAP_SUFFIX_SENSITIVITY_RUNNING`.
Training and E2E remain blocked.
