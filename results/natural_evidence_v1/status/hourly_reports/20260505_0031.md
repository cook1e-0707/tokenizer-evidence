# Hourly natural_evidence_v1 report

## Current phase
QWEN_CANDIDATE_SUPPLY_EXPANSION_RUNNING

Chimera access was restored with `ssh chimera`. Job 834777 is still running on
`chimera21`, so downstream gates remain blocked on its final outputs.

## Jobs
| Job ID | Name | State | Evidence |
|---|---|---|---|
| 834430 | nat-ev-qwen-clean8192 | LAST_KNOWN_COMPLETED 0:0 | recorded in prior state |
| 834431 | nat-ev-llama-clean8192 | LAST_KNOWN_COMPLETED 0:0 | recorded in prior state |
| 834777 | nat-ev-qwen-candx | RUNNING | `squeue` shows running on `chimera21`; `scontrol` runtime was 00:41:50 with 24h limit |

## Artifacts found
| Artifact | Status | Evidence |
|---|---|---|
| Qwen candidate expansion static validation | COMPLETE | remote summary exists and `passed=true` |
| dense Qwen top-k candidates | RUNNING | `qwen_topk_candidates.jsonl` not written yet |
| Qwen 4-way strict bank after expansion | NEEDS_RESULTS | strict bank entries and manifest are not written yet |
| Qwen candidate expansion final summary | NEEDS_RESULTS | summary is written only after candidate scoring and strict rebuild |

## Gate status
| Gate | Status | Evidence | Action |
|---|---|---|---|
| Chimera job status access | PASS | `ssh chimera` reached `chimerahead.umb.edu`; `squeue`, `sacct`, and `scontrol` worked | continue remote checks |
| Qwen candidate expansion job 834777 | RUNNING | H200 allocated, static validation passed, model checkpoint loaded | wait |
| Qwen 4-way strict bank target coverage after expansion | NEEDS_RESULTS | strict bank manifest is missing while job runs | inspect after completion |
| counterfactual compatibility | NEEDS_RESULTS | expanded bank is not complete | do not run |
| Qwen E2E pilot | TODO_AFTER_RESULTS | required bank, compatibility, null, and reconstructability gates are not passed | forbidden |

## Actions executed this run
- Retried `ssh chimera` as requested by the user.
- Checked `squeue`, `sacct`, and `scontrol show job 834777`.
- Inspected the expansion root for static validation, candidate JSONL, bank manifest, and final summary.
- Tailed job stdout/stderr to confirm the job reached model scoring.
- No experiment state-changing action was executed; no training or new GPU job was started.

## Red flags
- None in the natural_evidence_v1 main line this run.
- Do not treat missing candidate/bank files as failure while job 834777 is still running.

## Next allowed action
Wait for job 834777 to finish, then inspect:
- dense Qwen candidate row count;
- rebuilt Qwen 4-way strict bank accepted entries;
- whether accepted entries reach 24576 under the strict balance gate.

Do not start training, counterfactual scoring, Qwen 8-way rebuild, or Llama
rebuild while job 834777 is running.

## Claims still forbidden
- 24,576 fingerprints
- stealth guarantee
- full FAR
- robust to all paraphrasing
- cross-family generality
- superiority over Scalable/Perinucleus

## State update
`docs/natural_evidence_v1/AUTOMATION_STATE.md` and
`results/natural_evidence_v1/status/gate_status.json` now record Chimera access
as restored and job 834777 as running.
