# Hourly natural_evidence_v1 report

## Current phase
`QWEN_ACTUAL_PREFIX_REFERENCE_SCORING_READY_BLOCKED_BY_CHIMERA_DNS`.

The closest controlling boundary remains the Stage 4 real-integration spec plus
the `natural_evidence_v1` protocol docs. For this namespace, the natural
context-conditioned evidence docs supersede the old structured evidence-block
path, which remains frozen as a controlled sanity check.

## Jobs
- Local `squeue -u $USER`: unavailable (`command not found`).
- Local `sacct -u $USER --format=JobID,JobName,State,Elapsed,ExitCode -S now-2days`:
  unavailable (`command not found`).
- `/hpcstor6` is not mounted locally.
- `ssh chimera` failed DNS resolution for `chimerahead.umb.edu` on the first
  attempt and three retries.
- Active Slurm jobs and remote output paths are unverified this run.

No Slurm job was submitted.

## Artifacts found
- Local required Phase A JSONL/bank paths are missing or empty:
  - `results/natural_evidence_v1/reference_outputs/qwen_reference_outputs.jsonl`
  - `results/natural_evidence_v1/reference_outputs/llama_reference_outputs.jsonl`
  - `results/natural_evidence_v1/reference_candidates/qwen_topk_candidates.jsonl`
  - `results/natural_evidence_v1/reference_candidates/llama_topk_candidates.jsonl`
  - `results/natural_evidence_v1/bucket_banks/qwen_bucket_bank_entries.jsonl`
  - `results/natural_evidence_v1/bucket_banks/llama_bucket_bank_entries.jsonl`
- Local status artifacts exist for verifier alignment diagnosis, static-bucket
  salvage, and the actual-prefix scoring plan.
- Last-known state still records job 844494 as complete with 57164 Qwen
  actual-prefix scoring rows, but remote artifacts could not be reverified.

## Gate status
| Gate | Status | Evidence | Action |
|---|---|---|---|
| Repo status | DIRTY_EXPECTED | `main` at `e58308a Set natural bank target to 24000`; existing dirty worktree | Do not revert unrelated work |
| Old-route keyword scan | NO_RED_FLAG | matches are denylist config, guardrail tests, status notes/docs, and payload variable names; no new carrier-block output route found | Continue natural route only |
| Static validation | PASS | `python3 scripts/natural_evidence_v1/validate_static.py --config configs/natural_evidence_v1/pilot.yaml` passed | No static fix needed |
| Wrapper syntax | PASS | `py_compile` passed for scorer/validator; `bash -n` passed for `actual_prefix_reference_scoring.sbatch` | Wrapper remains locally valid |
| Chimera access | BLOCKED_DNS | `ssh chimera` failed DNS after first attempt plus three retries | Do not submit job this run |
| Local Phase A artifacts | MISSING_LOCAL | required local JSONL/bank files absent or empty; `/hpcstor6` not mounted | Treat remote artifacts as unverified |
| Qwen actual-prefix reference scoring | READY_BUT_NOT_SUBMITTED | allowlist action enabled, but queue/output checks could not run | Retry Chimera before submission |
| Qwen diagnostic E2E | BLOCKED | actual-prefix scoring, compatibility filtering, bucketization, and audit absent | Do not rerun eval |
| Llama / 8-way / full matrix | BLOCKED | Qwen actual-prefix repair unresolved | Do not start |

## Actions executed this run
- Read automation memory, repo guardrails, natural protocol docs, state docs,
  allowlist, gate status, and the last hourly report.
- Checked git status, branch, last commit, and natural_evidence_v1 path presence.
- Searched for old explicit evidence-block keywords in natural_evidence_v1 code,
  configs, docs, and tests.
- Checked local Phase A artifact paths and local `/hpcstor6` mount status.
- Ran local static validation, `py_compile`, and `bash -n`.
- Attempted Chimera access using `ssh chimera`; DNS failed after four attempts.
- Wrote this report and refreshed persistent status only.

No experiment state-changing action was executed. No GPU scoring job, training,
E2E eval, Llama job, Qwen 8-way job, full matrix, FAR aggregation, or paper
claim edit was executed.

## Red flags
- Chimera active jobs and remote scoring output state are unverified this run.
- The allowlisted Qwen actual-prefix reference scoring job must not be submitted
  until `ssh chimera`, Slurm queue/accounting, and output-existence checks
  succeed.
- Static-bucket salvage has already failed with no payload recovery and low
  protected symbol rate; true actual-prefix scoring is still the next repair
  step.
- No paper-facing Qwen E2E gate is passed, and no full FAR result exists.

## Next allowed action
Retry Chimera access. If `ssh chimera`, `squeue`, `sacct`, and remote
output-existence checks succeed and no conflicting natural_evidence_v1 job or
completed actual-prefix scoring output exists, submit exactly one allowlisted
GPU scoring job:

```bash
START_ACTUAL_PREFIX_SCORING=1 DRY_RUN_ONLY=0 \
sbatch scripts/natural_evidence_v1/slurm/actual_prefix_reference_scoring.sbatch
```

This is Qwen reference-model candidate scoring only: candidate_top_k=64,
bucket_count=4, no training, no E2E eval, and no payload-recovery claim.

## Claims still forbidden
- 24,576 fingerprints
- stealth guarantee
- full FAR
- robust to all paraphrasing
- cross-family generality
- superiority over Scalable/Perinucleus

## State update
`current_phase` remains
`QWEN_ACTUAL_PREFIX_REFERENCE_SCORING_READY_BLOCKED_BY_CHIMERA_DNS`.
`last_checked_time` is `2026-05-06T10:03:01Z`.
`next_allowed_action` remains
`retry_chimera_then_submit_one_allowlisted_qwen_actual_prefix_reference_scoring_gpu_job_topk64_no_training`.
`last_state_changing_action` records that no experiment state-changing action
was executed during this run.
