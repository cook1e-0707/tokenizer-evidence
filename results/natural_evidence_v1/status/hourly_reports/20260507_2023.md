# Hourly natural_evidence_v1 report

## Current phase

`POST_846699_DECODER_ORACLE_SUBSTITUTION_READY`.

## Jobs

Slurm job `847652` (`nat-ev-qwen-tfprob`) completed on DGXA100/chimera13 in
`01:07:52` with `ExitCode=0:0`. This was an artifact-only teacher-forced
bucket-mass probe. It did not train, generate, rerun E2E, or produce a
paper-facing claim.

## Artifacts found

Local copy:

```text
results/natural_evidence_v1/status/qwen_natural_e2e_eval_846699_teacher_forced_bucket_mass_probe/
```

Files:

```text
qwen_846699_teacher_forced_bucket_mass_probe_summary.json
qwen_846699_teacher_forced_bucket_mass_by_condition.csv
qwen_846699_teacher_forced_bucket_mass_by_slice.csv
qwen_846699_teacher_forced_bucket_mass_positions.jsonl
```

Row counts: 143,160 position rows, 791 by-slice CSV rows, 11 by-condition CSV
rows.

## Gate status

| Gate | Status | Evidence | Action |
|---|---|---|---|
| Teacher-forced bucket-mass probe | COMPLETE_SMALL_LIFT | protected mean target candidate mass `0.410354`; base `0.406997`; task-only `0.405440` | Treat as weak target-direction learning only |
| Free-generation survival | FAIL | prior on-policy compatible hit `1885/372216`, target hit `299/143160` | Do not rerun E2E |
| Frame completion | FAIL_OBSERVED | prior observed complete frames `0`; max observed slots/frame `1` | Decoder oracle substitution next |
| Payload recovery | FAIL | 846699 decode rows all `insufficient_symbols` | No positive claim |
| FAR/null claim | BLOCKED | positive channel did not recover | Do not aggregate FAR |

## Actions executed this run

- Verified job `847652` completed 0:0.
- Synced teacher-forced bucket-mass probe artifacts from Chimera to local
  `results/`.
- Updated `AUTOMATION_STATE.md`, `README.md`, `next_step_codex_plan.md`,
  `eligible_positions_sparse_diagnosis.md`, and `gate_status.json`.

## Red flags

- Protected LoRA has only a small teacher-forced target-mass lift over base and
  task-only; this does not explain away the sub-1% free-generation survival.
- Current protocol still has no payload recovery and no complete observed
  variable-radix frame.

## Next allowed action

Run artifact-only decoder oracle substitution. Replace observed tokens with
committed target bucket digits under the current variable-radix frame schedule
and test whether the evaluator can recover when every committed position emits
the correct bucket.

Do not submit new training, E2E rerun, Llama, same-family null, sanitizer, or
paper-facing positive-claim work before this diagnostic completes.

## Claims still forbidden

- 24,576 fingerprints
- stealth guarantee
- full FAR
- robust to all paraphrasing
- cross-family generality
- superiority over Scalable/Perinucleus

## State update

`current_phase=POST_846699_DECODER_ORACLE_SUBSTITUTION_READY`.
`qwen_846699_teacher_forced_bucket_mass_probe` is removed from
`remaining_NEEDS_RESULTS`; `qwen_846699_decoder_oracle_substitution` remains the
next required diagnostic.
