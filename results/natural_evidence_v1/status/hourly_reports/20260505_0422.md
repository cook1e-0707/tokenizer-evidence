# Hourly natural_evidence_v1 report

## Current phase
QWEN_CANDIDATE_SUPPLY_EXPANSION_SHARDED_RUNNING.

This was a user-confirmed recovery run for the slow Qwen dense-prefix candidate
supply expansion. No training, counterfactual compatibility, Qwen 8-way rebuild,
Llama rebuild, paper edit, or old compiled-path artifact overwrite was started.

## Jobs
- Before recovery, job 834777 (`nat-ev-qwen-candx`) was still RUNNING at about
  04:31:45 elapsed with no dense candidate JSONL, strict bank, or final summary
  output.
- Job 834777 was cancelled after explicit user confirmation.
- Replacement Slurm array 835092 was submitted with three independent 1-H200
  tasks:
  - 835092_0: RUNNING on `chimera21`.
  - 835092_1: RUNNING on `chimera21`.
  - 835092_2: RUNNING on `chimera21`.
- Short post-submit poll at `2026-05-05T04:22:04Z` showed all three shard tasks
  RUNNING and writing progress JSON.

## Artifacts found
- New sharded root:
  `/hpcstor6/scratch01/g/guanjie.lin001/tokenizer-evidence/natural_evidence_v1/phase_a_clean_decode8192_qwen_prefixdense_stride2_sharded3`
- Progress JSON exists for shards 0, 1, and 2 under `status/shards/`.
- Streaming shard candidate JSONL files exist under `reference_candidates/shards/`.
- Initial progress counts at the short poll:
  - shard 0: `output_rows=256`, `processed_records=7`, `total_records=2731`.
  - shard 1: `output_rows=256`, `processed_records=8`, `total_records=2731`.
  - shard 2: `output_rows=256`, `processed_records=7`, `total_records=2730`.

## Gate status
| Gate | Status | Evidence | Action |
|---|---|---|---|
| User-confirmed cancellation | PASS_EXECUTED | User requested cancellation and 3-shard H200 replacement | Old job 834777 cancelled |
| Sharded scorer code | PASS_SYNCED | Remote venv Python 3.12 `py_compile` passed | Use sharded scorer |
| Sharded Slurm script | PASS_SYNCED | Remote `bash -n` passed | Array submitted |
| Qwen candidate expansion shards | RUNNING | 835092_0, 835092_1, 835092_2 running | Wait for completion |
| Streaming progress | PASS | Progress JSON and shard JSONL files started | Monitor progress files |
| Shard merge | NEEDS_RESULTS | Shards are incomplete | Do not merge yet |
| Qwen strict bank rebuild | NEEDS_RESULTS | Needs merged candidate JSONL | Run only after all shards complete |
| Qwen E2E pilot | BLOCKED | Bank/compatibility/null gates incomplete | Do not start training |

## Actions executed this run
- Patched `scripts/natural_evidence_v1/score_reference_candidates.py` to support
  `--shard-index`, `--shard-count`, streaming JSONL output, and progress JSON.
- Added `scripts/natural_evidence_v1/slurm_qwen_candidate_supply_expansion_sharded.sbatch`.
- Added focused tests for shard selection and sharded sbatch scope.
- Synced the scorer and sharded sbatch to Chimera.
- Cancelled Slurm job 834777.
- Submitted Slurm array 835092 as three 1-H200 Qwen candidate-scoring shards.
- Updated this report, `docs/natural_evidence_v1/AUTOMATION_STATE.md`,
  `results/natural_evidence_v1/status/gate_status.json`, and automation memory.

## Red flags
- No new natural-output explicit evidence block route was introduced.
- The sharded array only scores Qwen candidate supply; it does not build a bank,
  run counterfactual compatibility, or start model training.
- Local `pytest` is unavailable (`No module named pytest`), so local validation
  used `py_compile`, `bash -n`, and direct shard-helper assertions.

## Next allowed action
Wait for all three 835092 shards to complete. Then concatenate the shard JSONL
files into a single Qwen dense candidate JSONL and run only the CPU strict Qwen
4-way bank build against the 24000 target.

## Claims still forbidden
- 24,576 fingerprints
- stealth guarantee
- full FAR
- robust to all paraphrasing
- cross-family generality
- superiority over Scalable/Perinucleus

## State update
`current_phase` is now `QWEN_CANDIDATE_SUPPLY_EXPANSION_SHARDED_RUNNING`.
`known_slurm_jobs` marks job 834777 as cancelled and array tasks 835092_0,
835092_1, and 835092_2 as running. `next_allowed_action` is waiting for shard
completion, then merge and strict Qwen 4-way bank build.
