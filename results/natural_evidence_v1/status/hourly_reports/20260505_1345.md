# Hourly natural_evidence_v1 report

## Current phase
QWEN_CANDIDATE_SUPPLY_EXPANSION_SHARDS_COMPLETED_NEEDS_MERGE

## Chimera access diagnosis
Root cause: the local `chimera` SSH alias targeted the brittle CNAME
`chimera.umb.edu`. Hourly automation repeatedly failed while resolving that
name, even after DNS warm-up and three retries. Direct access to the canonical
host `chimerahead.umb.edu` succeeds.

Fix applied locally: `~/.ssh/config` now sets `Host chimera` to
`HostName chimerahead.umb.edu` with `ConnectTimeout 10` and
`ConnectionAttempts 3`.

Verification:

```text
ssh -G chimera -> hostname chimerahead.umb.edu
ssh chimera 'hostname' -> chimerahead.umb.edu
```

## Jobs
| Job | Status | Evidence |
|---|---|---|
| 835092_0 | COMPLETED 0:0 | `sacct`; progress JSON `status=complete`, output_rows=112301 |
| 835092_1 | COMPLETED 0:0 | `sacct`; progress JSON `status=complete`, output_rows=110786 |
| 835092_2 | COMPLETED 0:0 | `sacct`; progress JSON `status=complete`, output_rows=111684 |

The corresponding shard candidate JSONL files have 112301, 110786, and 111684
rows and are ready for merge.

## Gate status
| Gate | Status | Action |
|---|---|---|
| Chimera job status access | PASS | canonical-host SSH fix verified |
| Qwen candidate supply shards | PASS_COMPLETED | merge shard candidates next |
| Qwen 4-way strict target coverage | NEEDS_RESULTS | run CPU strict bank build after merge |

## Actions executed this run
- Updated local SSH alias for `chimera`.
- Updated Chimera SSH reliability documentation.
- Updated automation state and gate status.
- No experiment job, training, counterfactual scoring, Qwen 8-way rebuild, or
  Llama rebuild was started.

## Next allowed action
Concatenate the three completed Qwen dense-prefix candidate shard JSONL files,
then run only the CPU strict Qwen 4-way bank build against the 24000
opportunity-entry target.
