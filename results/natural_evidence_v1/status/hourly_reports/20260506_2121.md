# Hourly natural_evidence_v1 report

## Current phase
QWEN_NATURAL_E2E_PROOF_OF_LIFE_TRAINING_SUBMITTED_RUNNING_OR_PENDING

## Jobs
- 846585 `nat-ev-qwen-nat-e2e`: submitted to `DGXA100`; initial state `PENDING(Resources)`, then `RUNNING` on `chimera13`; latest check shows elapsed 00:04:03.
- No second Slurm job was submitted.

## Artifacts found
- Training wrapper, five-arm eval wrapper, proof gate JSON, P0421/P1729 train artifacts, and 846443/846507 dry-run outputs are present on Chimera.
- No 846585 result artifact exists yet.

## Gate status
| Gate | Status | Evidence | Action |
|---|---|---|---|
| Explicit approval | PASS | user approved Qwen proof-of-life training with fixed payloads/seeds/budgets/MAX_STEPS/start flags | submit one job only |
| H200 availability | FAIL_CURRENTLY_DOWN | `pomplun` is up, but only node `chimera21` is `down` | do not submit H200 job now |
| DGXA100 submission | RUNNING | job 846585 submitted with `DRY_RUN_ONLY=0`, `START_QWEN_NATURAL_E2E=1`; latest `squeue`/`sacct` shows RUNNING on `chimera13` | monitor only |
| Training result | NEEDS_RESULTS | job has not completed and no training/eval result has been reviewed | wait for Slurm completion |

## Actions executed this run
- Checked H200/pomplun status first.
- Submitted exactly one approved Qwen natural proof-of-life training job, 846585.
- Performed one follow-up status check; job 846585 is running on DGXA100.
- Updated persistent state and gate status.

## Red flags
- `pomplun` still has no usable H200 node despite partition being administratively up.
- Multi-H200 was not requested because the trainer is a single-process LoRA path.

## Next allowed action
Monitor Slurm job 846585 only. Do not submit any additional training or eval job until it completes and artifacts are reviewed.

## Claims still forbidden
- 24,576 fingerprints
- stealth guarantee
- full FAR
- robust to all paraphrasing
- cross-family generality
- superiority over Scalable/Perinucleus

## State update
Training is running but no result exists yet. This is not payload recovery and not an E2E evaluation result.
