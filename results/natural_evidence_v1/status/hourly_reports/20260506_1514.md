# Hourly natural_evidence_v1 report

## Current phase
QWEN_ACTUAL_PREFIX_REFERENCE_SCORING_RUNNING.

This run continued the actual-prefix repair path after Chimera SSH was fixed by
pinning the alias to `158.121.247.54` with `HostKeyAlias chimerahead.umb.edu`.
The action taken is reference-model top-k scoring at actual generated prefixes;
it is not training, not E2E evaluation, and not payload recovery.

## Jobs
- Chimera access: PASS via `ssh chimera`; remote hostname returned
  `chimerahead.umb.edu`.
- Pre-submit queue: no active `nat-ev-*` jobs for the user.
- Submitted: job `845195`, `nat-ev-qwen-apscore`, partition `DGXA100`, account
  `pi_yinxin.wan`, QOS `scavenger_unlim`, `gres/gpu:A100:1`.
- Immediate post-submit status was `PENDING`, reason `Priority`; the final
  check in this run shows job `845195` is now `RUNNING` on `chimera12`.
- Initial submit attempt failed before job creation because the wrapper used
  `gpu:a100:1`; the wrapper was corrected to `gpu:A100:1`, synced to Chimera,
  and remote validation passed before resubmission.

## Artifacts found
- Actual-prefix scoring input:
  `/hpcstor6/scratch01/g/guanjie.lin001/tokenizer-evidence/natural_evidence_v1/diagnostic_high_risk_qwen_e2e/eval/qwen_diagnostic_e2e_eval_844121/actual_prefix_scoring_plan_v1/actual_prefix_scoring_input.jsonl`
  exists with 57164 rows.
- Actual-prefix scoring manifest:
  `/hpcstor6/scratch01/g/guanjie.lin001/tokenizer-evidence/natural_evidence_v1/diagnostic_high_risk_qwen_e2e/eval/qwen_diagnostic_e2e_eval_844121/actual_prefix_scoring_plan_v1/actual_prefix_scoring_manifest.json`
  exists.
- Target output JSONL, progress JSON, and summary JSON were absent before
  submission, so no overwrite occurred.
- Final check in this run found progress JSON present: 1024 / 57164 records
  processed, 1024 output rows, status `running`.

## Gate status
| Gate | Status | Evidence | Action |
|---|---|---|---|
| Chimera access | PASS | `ssh chimera` reached `chimerahead.umb.edu` | Continued gate |
| Old route keyword scan | NO_RED_FLAG | matches are denylist, validator, tests, or status text | No block |
| Static validation | PASS | local and remote validation passed | Continued gate |
| Slurm mail directives | PASS | wrapper contains `--mail-type=ALL` and configured mail user | Continued gate |
| A100 resource request | PASS_AFTER_FIX | wrapper now requests `gpu:A100:1`; Slurm accepted job | Submitted one job |
| Active job conflict | PASS | pre-submit `squeue` had no active `nat-ev-*` job | Submitted one job |
| Output overwrite guard | PASS | target output/progress/summary paths did not exist | Submitted one job |
| Qwen actual-prefix reference scoring | RUNNING | job `845195` on `chimera12`; 1024 / 57164 records processed | Monitor next |
| Training/E2E/Llama/full matrix | NOT_STARTED | no training/eval job submitted | Keep blocked |

## Actions executed this run
1. Verified local repo state, allowlist, static validation, wrapper syntax, mail
   directives, and old-route keyword scan.
2. Verified Chimera queue, recent Slurm history, actual-prefix scoring input,
   manifest, and absence of target outputs.
3. Fixed the actual-prefix scoring wrapper GRES request from `gpu:a100:1` to
   `gpu:A100:1`, synced the wrapper to Chimera, and revalidated remotely.
4. Submitted exactly one allowlisted Qwen actual-prefix reference-model scoring
   Slurm job: `845195`.

## Red flags
- Job `845195` is running; scoring outputs are partial and still NEEDS_RESULTS.
- The failed first submit attempt created no job, but it exposed a wrapper GRES
  typo that is now fixed.
- This is not payload recovery, not null rejection, not FAR, and not a
  natural-output success result.

## Next allowed action
Monitor job `845195`. If it completes successfully, inspect
`qwen_actual_prefix_reference_scoring_summary.json`,
`qwen_actual_prefix_reference_scoring_progress.json`, and
`qwen_actual_prefix_topk_candidates.jsonl`; then run actual-prefix bucketization
and compatibility audit only if the scoring outputs are complete.

Do not start LoRA training, E2E eval, Llama, Qwen 8-way, or a full matrix.

## Claims still forbidden
- 24,576 fingerprints
- stealth guarantee
- full FAR
- robust to all paraphrasing
- cross-family generality
- superiority over Scalable/Perinucleus

## State update
Current phase advanced from
`QWEN_ACTUAL_PREFIX_REFERENCE_SCORING_READY_BLOCKED_BY_CHIMERA_DNS` to
`QWEN_ACTUAL_PREFIX_REFERENCE_SCORING_RUNNING`.
Known job `845195` was added as the single submitted allowlisted GPU scoring
job for this run.
