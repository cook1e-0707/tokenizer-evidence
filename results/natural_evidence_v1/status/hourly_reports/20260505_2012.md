# Hourly natural_evidence_v1 report

## Current phase
QWEN_COMPATIBILITY_BANK_SIDE_VIABILITY_PASSED_PENDING_DENSITY_AND_PRE_NULL

## Decision
The raw 24,000 opportunity-entry value is no longer a training-start gate. It is
only a static raw-opportunity scaling placeholder and must not be compared to
Scalable Fingerprinting's 24,576 implanted fingerprint identities.

## Evidence
The probability-preserving compatibility dry-run reports:

- min1-compatible Qwen 4-way entries: 2327,
- configured-min / min2-compatible entries: 243,
- probability-gated high-confidence entries: 177.

The min1 and min2 counts pass the bank-side Qwen viability gate
(min1 >= 1500 and min2 >= 200). The probability-gated count is still low and
should be treated as a final-main diagnostic, not an immediate E2E blocker.

## Gate status
| Gate | Status | Action |
|---|---|---|
| Raw 24,000 entry count | DIAGNOSTIC_ONLY | no longer blocks training by itself |
| Qwen bank-side compatibility viability | PASS_MIN1_MIN2_PILOT_GATE | continue to density/null checks |
| Held-out/organic compatible density | NEEDS_RESULTS | run next |
| Raw/wrong-key pre-null | NEEDS_RESULTS | run next |
| Qwen E2E pilot | BLOCKED_PENDING_DENSITY_AND_PRE_NULL | prepare only after checks are not high risk |

## Next allowed action
Run held-out/organic compatible-density checks and raw/wrong-key pre-null checks
for the Qwen 4-way min1-compatible bank. Do not launch Llama, Qwen 8-way, or the
full training matrix.

## Claim control
Do not claim 24,000 fingerprints, final robustness, full FAR, or cross-family
generality. The current claim is only that Qwen bank-side compatibility is
sufficient to evaluate a controlled viability pilot after remaining pre-null and
density gates.
