# Hourly natural_evidence_v1 report

## Current phase
QWEN_ACTUAL_PREFIX_REFERENCE_SCORING_READY_BLOCKED_BY_CHIMERA_DNS

The reviewed next action remains Qwen actual-prefix reference-model top-k
scoring. This run did not submit it because Chimera DNS resolution failed before
Slurm status and output-existence checks could be verified.

## Jobs
- No new Slurm job was submitted.
- Local `squeue` and `sacct` are unavailable.
- `ssh chimera` resolves through the configured alias
  `chimerahead.umb.edu`, but the first attempt plus three non-interactive
  retries all failed at DNS resolution.
- Last verified relevant job remains 844494, completed 0:0 at the prior check;
  current Chimera queue state is unverified this run.

## Artifacts found
- Local state/report artifacts exist under `results/natural_evidence_v1/status`.
- Local Phase A JSONL paths under `results/natural_evidence_v1/reference_outputs`,
  `reference_candidates`, and `bucket_banks` are missing or empty; remote Phase A
  artifacts remain last-known complete from previous Chimera checks but were not
  reverified this run.
- The actual-prefix scoring plan is last-known complete from job 844494:
  57164 scoring prefixes from 14336 retained generated outputs.
- The actual-prefix scoring wrapper and scorer are present locally and passed
  local static validation, `py_compile`, and `bash -n`.

## Gate status
| Gate | Status | Evidence | Action |
|---|---|---|---|
| Repo status | DIRTY_EXPECTED | existing natural_evidence_v1 worktree changes remain | Do not revert unrelated work |
| Old-route keyword scan | NO_RED_FLAG | matches are denylist/tests/status/payload variables, not a carrier-block output path | Continue natural route only |
| Static validation | PASS | `.venv/bin/python scripts/natural_evidence_v1/validate_static.py` passed with no errors | Keep wrapper eligible |
| Chimera access | BLOCKED_DNS | `ssh chimera` failed DNS after documented retries | Do not submit job this run |
| Qwen actual-prefix reference scoring | READY_BUT_NOT_SUBMITTED | allowlist action enabled; wrapper reviewed; queue/output unverified | Retry Chimera, then submit one scoring job if clear |
| Qwen diagnostic E2E | BLOCKED | actual-prefix scoring, compatibility filtering, bucketization, and audit absent | Do not rerun eval |
| Llama / 8-way / full matrix | BLOCKED | Qwen repair unresolved | Do not start |

## Actions executed this run
- Read automation memory, repo guardrails, README, automation state, protocol
  commitment, run allowlist, and gate status.
- Ran `git status --short`, branch, and last commit checks.
- Searched natural_evidence_v1 paths for old explicit evidence-block keywords.
- Ran local static validation with the project venv and wrote the temporary
  summary to `/private/tmp`.
- Ran local `py_compile` and `bash -n` for the actual-prefix scorer/wrapper.
- Attempted Chimera Slurm checks; DNS resolution failed, so no experiment
  state-changing action was executed.

## Red flags
- Chimera access is unverified this run; active jobs and remote scoring outputs
  could not be checked.
- Do not submit Qwen actual-prefix scoring until `ssh chimera`, `squeue`, and
  remote output existence checks succeed.
- No E2E eval, LoRA training, Llama, Qwen 8-way, full matrix, FAR aggregation,
  or paper claim edit is allowed from this state.

## Next allowed action
Retry Chimera access. If Slurm shows no conflicting active natural_evidence_v1
job and the actual-prefix scoring output directory has no completed output,
submit exactly one allowlisted GPU scoring job:

```bash
START_ACTUAL_PREFIX_SCORING=1 DRY_RUN_ONLY=0 \
sbatch scripts/natural_evidence_v1/slurm/actual_prefix_reference_scoring.sbatch
```

This is Qwen reference-model candidate scoring only: candidate_top_k=64,
bucket_count=4, no training, no E2E eval, no payload-recovery claim.

## Claims still forbidden
- 24,576 fingerprints
- stealth guarantee
- full FAR
- robust to all paraphrasing
- cross-family generality
- superiority over Scalable/Perinucleus

## State update
`current_phase` is
`QWEN_ACTUAL_PREFIX_REFERENCE_SCORING_READY_BLOCKED_BY_CHIMERA_DNS`.
`next_allowed_action` remains
`retry_chimera_then_submit_one_allowlisted_qwen_actual_prefix_reference_scoring_gpu_job_topk64_no_training`.
