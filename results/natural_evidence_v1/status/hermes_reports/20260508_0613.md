# Hermes supervision report

phase:
POST_846699_BRANCH_AWARE_SCORE_INTERPRETATION_COMPLETE

active_jobs:
Not checked in this notification-helper correction. No Slurm job was submitted.

completed_jobs:
Latest completed diagnostic remains 848414 branch-aware compatibility proxy
scoring. No experiment artifact was advanced in this tick.

artifact_changes:
- Updated `scripts/natural_evidence_v1/hermes_notify.py` to load
  `/Users/guanjie/.hermes/.env` by default.
- Added Hermes alias support for `TELEGRAM_HOME_CHANNEL`,
  `EMAIL_HOME_ADDRESS`, `EMAIL_SMTP_HOST`, `EMAIL_SMTP_PORT`,
  `EMAIL_ADDRESS`, and `EMAIL_PASSWORD`.
- Updated Hermes supervision docs/state to document the default env-file path.
- Added a test for env-file alias loading.

gate_changes:
Hermes notification remains a hard supervision gate. The helper now reads the
existing Hermes env file by default, so future project-advancing ticks should
use the stored TG/email configuration without requiring variables to be exported
in the Codex shell.

red_flags:
This tick changes notification plumbing only. Training, generation, E2E rerun,
FAR aggregation, Llama, same-family null, sanitizer, and paper-facing positive
claims remain forbidden.

telegram_notification:
Sent through `scripts/natural_evidence_v1/hermes_notify.py --strict` using
`/Users/guanjie/.hermes/.env`; notification JSON reports `status=SENT`.

email_notification:
Sent through `scripts/natural_evidence_v1/hermes_notify.py --strict` using
`/Users/guanjie/.hermes/.env`; notification JSON reports `status=SENT`.

notification_json:
`results/natural_evidence_v1/status/hermes_reports/20260508_0613_notification.json`

state_changing_action:
Notification helper/docs/state/tests only.

next_allowed_action:
After notification succeeds, design or run an artifact-only repaired
teacher-forced target-mass probe over the primary branch-aware candidates.

forbidden_actions_confirmed:
Training, generation, Qwen E2E rerun, Llama, same-family null, sanitizer, FAR
aggregation, and paper-facing positive claims remain forbidden.
