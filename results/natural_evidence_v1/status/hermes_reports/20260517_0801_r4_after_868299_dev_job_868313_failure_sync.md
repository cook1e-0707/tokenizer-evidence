# Hermes sync: R4 after-868299 dev job 868313 failed

phase:
`V2_R4_AFTER_868299_DEV_JOB_868313_FAILED_RUNTIME_ALLOWLIST_RACE_REPAIR_NEXT`

summary:

```text
Slurm array 868313 (nat-ev-v2-r4dev) reached a mixed failed state.

Failed shards:
  0..23
Completed shards:
  24..31

Root cause:
  runtime allowlist enabled-state race before immediate post-sbatch disablement.

Representative failed shard validator errors:
  - allowlist enabled entries must be empty during plan validation:
    ['v2_r4_after_868299_first_token_event_dev_diagnostic_h200']
  - allowlist entry enabled state must be False

Interpretation:
  This is a control-plane/runtime validation failure. It is not a model,
  tokenizer, controller, decoder, or H200 failure. Failed shards exited before
  generation. Completed partial shards are not adopted as a canonical 32-block
  dev diagnostic result.
```

recorded:

```text
results/natural_evidence_v2/status/r4_after_868299_first_token_event_dev_diagnostic_868313_failure_review_20260517/
docs/natural_evidence_v2/CURRENT_STATE.md
results/natural_evidence_v1/status/gate_status.json
results/natural_evidence_v2/status/gate_status.json
```

next_allowed_action:

```text
Patch runtime route validation to skip only allowlist enabled-state checks
during array runtime; keep submission preflights strict. After tests and
local/remote preflights pass, submit one repaired H200 32-shard dev diagnostic
replacement.
```

not_allowed:

```text
adopting partial 868313 outputs as a canonical dev result
paper-facing positive claim
training
Llama
same-family null
sanitizer
FAR
payload diversity claim
```
