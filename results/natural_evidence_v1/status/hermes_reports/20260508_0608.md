# Hermes supervision report

phase:
POST_846699_BRANCH_AWARE_SCORE_INTERPRETATION_COMPLETE

active_jobs:
Not checked in this notification-policy update. No new Slurm job was submitted.

completed_jobs:
Latest completed diagnostic remains 848414 branch-aware compatibility proxy
scoring; no experiment artifact was advanced in this tick.

artifact_changes:
- Added `scripts/natural_evidence_v1/hermes_notify.py` as the Telegram/email
  notification helper for Hermes progress ticks.
- Updated Hermes supervision docs/state so every project-advancing Hermes tick
  must notify the user through Telegram and email before Codex executes.
- Added tests for dry-run notification reporting and missing-configuration
  blocking.

gate_changes:
Hermes notification is now a hard supervision gate. A project-advancing Hermes
tick must use `scripts/natural_evidence_v1/hermes_notify.py --channels
telegram,email --strict`. If Telegram or email is missing, fails, or is only a
dry-run, Hermes must stop forward prompting and record a notification blocker.

red_flags:
Runtime Telegram/email credentials have not been proven in this tick. This
update only records the policy and helper; it does not claim a live notification
was delivered. The dry-run JSON reports missing `HERMES_TG_BOT_TOKEN`,
`HERMES_TG_CHAT_ID`, and `HERMES_EMAIL_TO` in the current Codex environment.

telegram_notification:
Required for future Hermes progress ticks. Not sent in this policy-update tick.

email_notification:
Required for future Hermes progress ticks. Not sent in this policy-update tick.

notification_json:
`results/natural_evidence_v1/status/hermes_reports/20260508_0608_notification_dry_run.json`

state_changing_action:
Documentation, status JSON, notification helper, and tests only.

next_allowed_action:
After Hermes Telegram/email notification is configured and succeeds, design or
run an artifact-only repaired teacher-forced target-mass probe over the primary
branch-aware candidates.

forbidden_actions_confirmed:
Training, generation, Qwen E2E rerun, Llama, same-family null, sanitizer, FAR
aggregation, and paper-facing positive claims remain forbidden.
